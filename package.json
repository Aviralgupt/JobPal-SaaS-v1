{
  "name": "jobpal-gemini",
  "version": "1.0.0",
  "description": "JobPal AI Resume Optimizer with Google Gemini",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
